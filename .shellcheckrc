# These are sourced bash rc files, not standalone scripts.
shell=bash
# These files source external runtime files (e.g. ~/.cargo/env, ~/.travis/travis.sh)
# that aren't present at lint time. Don't warn about following them.
disable=SC1090,SC1091
